---
import { getCollection } from 'astro:content';
import BlogSidebar from '../components/BlogSidebar.astro';
import BaseLayout from '../layouts/BaseLayout.astro';
import { sortPosts } from '../lib/blog';

const posts = sortPosts(await getCollection('blog'));
---

<BaseLayout title="sidebar.html" description="Generated sidebar page" canonicalPath="/sidebar.html">
  <section class="hero">
    <h1 class="page-title">sidebar.html</h1>
    <p class="page-intro">This static page can act as a standalone sidebar hub or be reused as a template fragment.</p>
  </section>

  <BlogSidebar posts={posts} />
</BaseLayout>
